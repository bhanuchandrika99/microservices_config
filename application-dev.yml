server:
  port: 8085
spring:
  application:
    name: CONFIG-DEV

  cloud:
    config:
      server:
        git:
          uri: https://github.com/bhanuchandrika99/microservices_config
          clone-on-start: true
