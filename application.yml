server:
  port: 8085
spring:
  application:
    name: CONFIG-DEFAULT

  cloud:
    config:
      server:
        git:
          uri: https://github.com/bhanuchandrika99/microservices_config
          clone-on-start: true
